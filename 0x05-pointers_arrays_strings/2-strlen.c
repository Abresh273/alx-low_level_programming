#include "main.h"
#include <stdio.h>

/**
 * _strlen - return length of a string
 *
 * @s : string input
 *
 * Return : Always 0
 *
 */


int _strlen(char *s)
{
	int a;
	a = strlen(*s);
	return (a);
}

