# include "main.h"


/**
 *
 * _atoi -  a function that convert a string to an integer.
 *
 * @s : input argument char
 *
 * Return : Always 0
 *
 */

int _atoi(char *s)
{
	int i;
	char a;
	for (i=0;s[i] != '\0'; i++)
	{
		a = s[i];
	}

	return (a);
}
