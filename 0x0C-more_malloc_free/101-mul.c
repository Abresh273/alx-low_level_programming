#include "main.h"
#include <stdlib.h>
#include <stdio.h>


/**
 * _memset - fills memory with a constant byte
 *
 * @s: input pointer that represents memory block
 *     to fill
 * @b: characters to fill/set
 * @n: number of bytes to be filled
 *
 * Return: Always 0
*/

int main(int num0, int num1, int num2)
{
	if(num1> && num2 >0)
	{
		printf("%d",num1*num2)
	}
	else
	{
		printf("Error")
	}
	return (0);
}

